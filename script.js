const clamp=(n,a=0,b=1)=>Math.max(a,Math.min(b,n));const lerp=(a,b,t)=>a+(b-a)*t;function prog(el){const r=el.getBoundingClientRect();return clamp(-r.top/(el.offsetHeight-innerHeight))}function scrollFx(){document.querySelectorAll('.scroll-stage').forEach(s=>{const p=prog(s);if(s.classList.contains('origin')){s.querySelector('.origin-copy').style.transform=`translateY(${lerp(70,-30,p)}px)`;s.querySelectorAll('.chaos div').forEach((x,i)=>{x.style.transform+=` scale(${lerp(.75,1.08,clamp((p-i*.04)*1.8))})`;x.style.opacity=clamp((p-.1-i*.04)*3)})}if(s.classList.contains('noble')){s.querySelector('.noble-art').style.transform=`scale(${lerp(1.15,1,p)}) translateX(${lerp(8,0,p)}%)`;s.querySelector('.noble-copy').style.transform=`translateY(${lerp(55,-15,p)}px)`}if(s.classList.contains('sidekick')){s.querySelector('.bot1').style.transform=`translate(${lerp(100,0,p)}px,${lerp(60,-10,p)}px) rotate(${lerp(8,-2,p)}deg)`;s.querySelector('.side-copy').style.transform=`translateY(${lerp(70,-20,p)}px)`}if(s.classList.contains('transformation')){const b=s.querySelector('.before'),a=s.querySelector('.after');b.style.transform=`translateX(${lerp(0,-35,p)}px) rotate(${lerp(0,-2,p)}deg)`;a.style.transform=`translateX(${lerp(35,0,p)}px) rotate(${lerp(2,0,p)}deg)`}})}addEventListener('scroll',scrollFx,{passive:true});scrollFx();const io=new IntersectionObserver(es=>es.forEach(e=>e.isIntersecting&&e.target.classList.add('on')),{threshold:.12});document.querySelectorAll('.scene h2,.scene .panel-label,.villain-grid article,.arsenal-grid>div,.countyline,.final-copy').forEach(x=>{x.classList.add('reveal');io.observe(x)});
